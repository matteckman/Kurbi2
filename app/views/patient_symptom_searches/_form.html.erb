<%= form_for(@patient_symptom_search) do |f| %>
  <% if @patient_symptom_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_symptom_search.errors.count, "error") %> prohibited this patient_symptom_search from being saved:</h2>

      <ul>
      <% @patient_symptom_search.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
