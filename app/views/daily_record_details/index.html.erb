<h1>Listing daily_record_details</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @daily_record_details.each do |daily_record_detail| %>
  <tr>
    <td><%= link_to 'Show', daily_record_detail %></td>
    <td><%= link_to 'Edit', edit_daily_record_detail_path(daily_record_detail) %></td>
    <td><%= link_to 'Destroy', daily_record_detail, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Daily record detail', new_daily_record_detail_path %>
