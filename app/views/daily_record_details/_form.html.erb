<%= form_for(@daily_record_detail) do |f| %>
  <% if @daily_record_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_record_detail.errors.count, "error") %> prohibited this daily_record_detail from being saved:</h2>

      <ul>
      <% @daily_record_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
