<%= form_for(@daily_record) do |f| %>
  <% if @daily_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_record.errors.count, "error") %> prohibited this daily_record from being saved:</h2>

      <ul>
      <% @daily_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
