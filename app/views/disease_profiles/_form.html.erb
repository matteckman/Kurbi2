<%= form_for(@disease_profile) do |f| %>
  <% if @disease_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disease_profile.errors.count, "error") %> prohibited this disease_profile from being saved:</h2>

      <ul>
      <% @disease_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
